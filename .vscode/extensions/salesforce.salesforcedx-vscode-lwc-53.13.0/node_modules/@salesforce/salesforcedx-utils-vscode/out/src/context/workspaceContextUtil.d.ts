import { Connection } from '@salesforce/core';
import * as vscode from 'vscode';
import { AuthUtil } from '..';
export interface OrgInfo {
    username?: string;
    alias?: string;
}
/**
 * Manages the context of a workspace during a session with an open SFDX project.
 */
export declare class WorkspaceContextUtil {
    protected static instance?: WorkspaceContextUtil;
    protected cliConfigWatcher: vscode.FileSystemWatcher;
    protected sessionConnections: Map<string, Connection>;
    protected onOrgChangeEmitter: vscode.EventEmitter<OrgInfo>;
    protected _username?: string;
    protected _alias?: string;
    readonly onOrgChange: vscode.Event<OrgInfo>;
    protected constructor();
    getAuthUtil(): AuthUtil;
    initialize(context: vscode.ExtensionContext): Promise<void>;
    static getInstance(forceNew?: boolean): WorkspaceContextUtil;
    getConnection(): Promise<Connection>;
    protected handleCliConfigChange(): Promise<void>;
    get username(): string | undefined;
    get alias(): string | undefined;
}
export declare function getLogDirPath(): string;
