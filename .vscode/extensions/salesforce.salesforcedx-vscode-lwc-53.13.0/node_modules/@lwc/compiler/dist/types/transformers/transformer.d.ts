import lwcClassTransformPlugin from "@lwc/babel-plugin-component";
import { NormalizedCompilerOptions, CompilerOptions } from "../compiler/options";
import { TemplateMetadata } from "./template";
import { MetadataCollector } from "../bundler/meta-collector";
import { SourceMap } from "../compiler/compiler";
export interface FileTransformerResult {
    code: string;
    metadata?: TemplateMetadata | lwcClassTransformPlugin.Metadata;
    map: SourceMap | null;
}
export declare type FileTransformer = (source: string, filename: string, options: NormalizedCompilerOptions, metadataCollector?: MetadataCollector) => FileTransformerResult | Promise<FileTransformerResult>;
export declare function transform(src: string, id: string, options: CompilerOptions): Promise<FileTransformerResult>;
export declare function getTransformer(fileName: string): FileTransformer;
export declare function transformFile(src: string, id: string, options: NormalizedCompilerOptions, metadataCollector?: MetadataCollector): Promise<FileTransformerResult>;
