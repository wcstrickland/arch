import { Location } from "../common-interfaces/location";
export interface ModuleImportLocation {
    name: string;
    location: Location;
}
export declare function collectImportLocations(code: string): {
    name: string;
    location: {
        start: number;
        length: number;
    };
}[];
