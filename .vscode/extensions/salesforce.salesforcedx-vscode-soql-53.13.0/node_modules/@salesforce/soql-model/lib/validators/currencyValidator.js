"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.isCurrencyLiteral = exports.CurrencyValidator = void 0;
/*
 * Copyright (c) 2021, salesforce.com, inc.
 * All rights reserved.
 * Licensed under the BSD 3-Clause license.
 * For full license text, see LICENSE.txt file in the repo root or https://opensource.org/licenses/BSD-3-Clause
 */
const messages_1 = require("../messages/messages");
const floatValidator_1 = require("./floatValidator");
class CurrencyValidator extends floatValidator_1.FloatValidator {
    validate(input) {
        const result = super.validate(input);
        if (result.isValid) {
            return result;
        }
        const isValid = isCurrencyLiteral(input);
        const message = isValid ? undefined : messages_1.Messages.error_fieldInput_currency;
        return { isValid, message };
    }
}
exports.CurrencyValidator = CurrencyValidator;
function isCurrencyLiteral(s) {
    return /^[a-zA-Z]{3}[+-]?[0-9]*[.]?[0-9]+$/.test(s.trim());
}
exports.isCurrencyLiteral = isCurrencyLiteral;
//# sourceMappingURL=currencyValidator.js.map