"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.StringValidator = void 0;
/*
 * Copyright (c) 2021, salesforce.com, inc.
 * All rights reserved.
 * Licensed under the BSD 3-Clause license.
 * For full license text, see LICENSE.txt file in the repo root or https://opensource.org/licenses/BSD-3-Clause
 */
const messages_1 = require("../messages/messages");
const validator_1 = require("./validator");
class StringValidator extends validator_1.Validator {
    validate(input) {
        const isValid = input.length >= 2 &&
            input.startsWith("'") &&
            input.endsWith("'") &&
            !this.isEscaped(input.substring(1, input.length - 1));
        const message = isValid ? undefined : messages_1.Messages.error_fieldInput_string;
        return { isValid, message };
    }
    isEscaped(input) {
        // test to see whether trailing single quote is escaped
        let isEscaped = false;
        for (let i = 0; i < input.length; i++) {
            const ch = input.charAt(i);
            if (isEscaped) {
                isEscaped = false;
            }
            else if (ch === '\\') {
                isEscaped = true;
            }
        }
        return isEscaped;
    }
}
exports.StringValidator = StringValidator;
//# sourceMappingURL=stringValidator.js.map