import { LogContext } from '../core/logContext';
import { DebugLogState } from './debugLogState';
export declare class UserDebugState implements DebugLogState {
    private readonly line;
    private message;
    constructor(fields: string[]);
    getMessage(): string;
    handle(logContext: LogContext): boolean;
    lookAheadAndAppend(logContext: LogContext): void;
}
