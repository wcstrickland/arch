import { DebugProtocol } from 'vscode-debugprotocol';
import { LineBreakpointInfo } from '.';
export declare class BreakpointUtil {
    private static instance;
    private lineNumberMapping;
    private typerefMapping;
    setValidLines(lineNumberMapping: Map<string, number[]>, typerefMapping: Map<string, string>): void;
    static getInstance(): BreakpointUtil;
    static setInstance(inputInstance: BreakpointUtil): void;
    hasLineNumberMapping(): boolean;
    getLineNumberMapping(): Map<string, number[]>;
    getTyperefMapping(): Map<string, string>;
    canSetLineBreakpoint(uri: string, line: number): boolean;
    createMappingsFromLineBreakpointInfo(lineBpInfo: LineBreakpointInfo[]): void;
    returnLinesForLoggingFromBreakpointArgs(bpArr: DebugProtocol.SourceBreakpoint[]): string;
    getTopLevelTyperefForUri(uriInput: string): string;
}
