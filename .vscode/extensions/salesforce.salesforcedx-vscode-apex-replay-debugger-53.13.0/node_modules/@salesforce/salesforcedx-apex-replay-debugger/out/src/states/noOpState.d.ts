import { LogContext } from '../core/logContext';
import { DebugLogState } from './debugLogState';
export declare class NoOpState implements DebugLogState {
    handle(logContext: LogContext): boolean;
}
