import { Connection } from '@salesforce/core';
import { SObjectSelector } from '../describe';
import { SObjectDefinitionRetriever, SObjectRefreshOutput } from '../types';
export declare class OrgObjectRetriever implements SObjectDefinitionRetriever {
    private describer;
    constructor(connection: Connection);
    retrieve(output: SObjectRefreshOutput): Promise<void>;
}
export declare class OrgObjectDetailRetriever implements SObjectDefinitionRetriever {
    private describer;
    private selector;
    constructor(connection: Connection, selector: SObjectSelector);
    retrieve(output: SObjectRefreshOutput): Promise<void>;
    private selectedTypes;
}
