import { SObjectCategory, SObjectDefinition, SObjectGenerator, SObjectRefreshOutput } from '../types';
export declare const INDENT = "    ";
export declare const APEX_CLASS_EXTENSION = ".cls";
export declare class FauxClassGenerator implements SObjectGenerator {
    private sobjectSelector;
    private relativePath;
    private declGenerator;
    constructor(selector: SObjectCategory, relativePath: string);
    private static fieldDeclToString;
    static commentToString(comment?: string): string;
    generate(output: SObjectRefreshOutput): void;
    generateFauxClass(folderPath: string, definition: SObjectDefinition): string;
    generateFauxClassText(definition: SObjectDefinition): string;
    private resetOutputFolder;
}
