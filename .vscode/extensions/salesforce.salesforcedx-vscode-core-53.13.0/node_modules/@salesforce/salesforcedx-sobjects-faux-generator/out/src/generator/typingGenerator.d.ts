import { SObject, SObjectDefinition, SObjectGenerator, SObjectRefreshOutput } from '../types';
export declare const TYPESCRIPT_TYPE_EXT = ".d.ts";
export declare class TypingGenerator implements SObjectGenerator {
    private declGenerator;
    constructor();
    generate(output: SObjectRefreshOutput): void;
    generateTypes(sobjects: SObject[], targetFolder: string): void;
    generateType(folderPath: string, definition: SObjectDefinition): string;
    private convertDeclarations;
    private isCollectionType;
    private convertDeclaration;
    private convertType;
}
