import { SObject, SObjectDefinition } from '../types';
export declare const MODIFIER = "global";
export declare class DeclarationGenerator {
    private static typeMapping;
    generateSObjectDefinitions(sobjects: SObject[]): SObjectDefinition[];
    generateSObjectDefinition(sobject: SObject): SObjectDefinition;
    private stripId;
    private capitalize;
    private getTargetType;
    private getReferenceName;
    private generateChildRelationship;
    private generateField;
}
