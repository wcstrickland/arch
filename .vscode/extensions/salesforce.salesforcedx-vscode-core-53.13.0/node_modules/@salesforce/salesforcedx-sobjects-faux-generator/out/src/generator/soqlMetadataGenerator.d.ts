import { SObjectCategory, SObjectGenerator, SObjectRefreshOutput } from '../types';
export declare class SOQLMetadataGenerator implements SObjectGenerator {
    private category;
    constructor(category: SObjectCategory);
    generate(output: SObjectRefreshOutput): void;
    private generateTypesNames;
    private generateMetadataForSObject;
    private resetOutputFolder;
}
