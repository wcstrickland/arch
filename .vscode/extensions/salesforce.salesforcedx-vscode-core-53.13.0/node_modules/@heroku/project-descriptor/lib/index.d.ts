import { ValidateFunction } from 'ajv';
export declare class ProjectDescriptor {
    validator: Promise<ValidateFunction>;
    constructor();
    parseFile(filepath: string): Promise<unknown>;
    parseData(data: any): Promise<unknown>;
    private readSchema;
    private createValidator;
}
