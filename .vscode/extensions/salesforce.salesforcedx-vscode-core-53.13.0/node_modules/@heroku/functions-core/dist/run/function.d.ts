import { AxiosResponse } from 'axios';
export interface RunFunctionOptions {
    url: string;
    payload: string;
    headers?: string[];
    structured?: boolean;
    targetusername?: string;
}
export declare function runFunction(options: RunFunctionOptions): Promise<AxiosResponse<any>>;
