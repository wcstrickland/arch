export declare const FUNCTIONS_DIR = "functions";
export declare const PROJECT_JSON = "sfdx-project.json";
export declare const SFDX_SDK_VERSION: string;
interface WriteTemplateConfig {
    fnDir: string;
    toFile: string;
    tplArgs: any;
    tplFile: string;
    options?: any;
}
interface TemplateConfig {
    fnDir: string;
    fnName: string;
    fnNameCased: string;
}
/**
 * Base template class.  Writes project files.
 */
declare abstract class Template {
    abstract get name(): string;
    write(tplConfig: TemplateConfig): void;
    get templateDir(): string;
    protected writeFunctionToml(tplConfig: TemplateConfig): void;
    protected abstract writeLanguageFiles(tplConfig: TemplateConfig): void;
    protected writeFileFromTemplate(writeTemplate: WriteTemplateConfig): void;
}
/**
 * Manages templates.
 */
declare class TemplateRegistry {
    private readonly templates;
    templateNames: Array<string>;
    registerTemplate(template: Template): void;
    getTemplate(name: string): Template | undefined;
}
export declare const TEMPLATE_REGISTRY: TemplateRegistry;
export {};
