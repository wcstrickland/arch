import { Range, TextDocument, TextEdit } from 'vscode-languageserver-types';
import { HTMLFormatConfiguration } from '../htmlLanguageService';
export declare function format(document: TextDocument, range: Range, options: HTMLFormatConfiguration): TextEdit[];
