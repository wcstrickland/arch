/**
 * Conventions:
 * _message: is for unformatted text that will be shown as-is to
 * the user.
 * _text: is for text that will appear in the UI, possibly with
 * decorations, e.g., $(x) uses the https://octicons.github.com/ and should not
 * be localized
 *
 * If ommitted, we will assume _message.
 */
export declare const messages: {
    command_error_help_text: string;
    session_language_server_error_text: string;
    session_no_entity_access_text: string;
    session_started_text: string;
    session_terminated_text: string;
    streaming_connected_text: string;
    streaming_disconnected_text: string;
    streaming_handshake_error_text: string;
    streaming_handshake_timeout_text: string;
    hotswap_warn_text: string;
    created_exception_breakpoint_text: string;
    removed_exception_breakpoint_text: string;
    idle_warn_text: string;
    idle_terminated_text: string;
    invalid_isv_project_config: string;
};
