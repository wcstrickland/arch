import { RequestService } from '@salesforce/salesforcedx-utils-vscode/out/src/requestService';
export declare class SessionService {
    private userFilter?;
    private requestFilter?;
    private entryFilter?;
    private project?;
    private sessionId;
    private connected;
    private readonly requestService;
    constructor(requestService: RequestService);
    withUserFilter(filter?: string): SessionService;
    withRequestFilter(filter?: string): SessionService;
    withEntryFilter(filter?: string): SessionService;
    forProject(project?: string): SessionService;
    isConnected(): boolean;
    getSessionId(): string;
    isApexDebuggerSessionId(id: string): boolean;
    start(): Promise<string>;
    stop(): Promise<string>;
    forceStop(): void;
}
